[package]
name = "jammars"
version = "0.1.0"
edition = "2021"

[dependencies]
macros = { path = "./proc-macros" }
rand = { version = "0.9", features = ["os_rng"] }
wyrand = "0.3"

[dev-dependencies]
terminal_size = "0.4"

[features]
seeded = []